# KAT tooling bundle

This bundle contains optional resonance graph tooling for KAT.

Copy the folders into your `klipper_KAT` repository:

```text
scripts/
optional/
docs/
```

Then make the scripts executable:

```bash
chmod +x scripts/*.sh
```

See:

```text
docs/resonance_tooling.md
```
